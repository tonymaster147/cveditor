# Compose-level secrets used by docker-compose.yml.
# Copy to .env on the VPS and fill in real values.

MYSQL_ROOT_PASSWORD=replace_with_strong_random
MYSQL_USER=cveditor
MYSQL_PASSWORD=replace_with_strong_random
